services:
  - type: web
    name: golf-card-game
    runtime: node
    buildCommand: npm run build:render
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
